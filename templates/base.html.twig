<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
<header>
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <a class="navbar-brand" href="{{ path('article_index')}}">Navbar</a>
            <div class="collapse navbar-collapse" id="navbarNav">
              <ul class="navbar-nav">
                <li class="nav-item active">
                  <a class="nav-link" href="{{ path('article_index') }}">Home</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="{{ path('article_admin_index') }}">Admin</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="{% if app.user %} {{path('app_profil')}} {%  else %} {{ path('app_login') }} {% endif %}">{% if app.user %}Profil {% else %} Se connecter {% endif %}
                    </a>
                  </li>
              </ul>
            </div>
          </nav>
</header>
        {% block body %}{% endblock %}


        {% block javascripts %}{% endblock %}
    </body>
</html>
